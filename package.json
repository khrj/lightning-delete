{
    "devDependencies": {
        "@prisma/cli": "2.14.0"
    },
    "dependencies": {
        "@prisma/client": "2.14.0",
        "@slack/oauth": "^2.0.0",
        "@slack/socket-mode": "^1.0.0",
        "@slack/web-api": "^6.0.0",
        "dotenv": "^8.2.0",
        "express": "^4.17.1"
    },
    "scripts": {
        "start": "node -r dotenv/config index.js",
        "generate-db": "prisma migrate dev --preview-feature",
        "reset-db": "prisma migrate reset --preview-feature"
    }
}
